{
    "name": "spelling-bee-extension",
    "version": "2.0.0",
    "description": "Chrome extension for the Spelling Bee game",
    "scripts": {
        "build": "esbuild popup.js --bundle --outfile=dist/popup_bundle.js",
        "watch": "esbuild popup.js --bundle --outfile=dist/popup_bundle.js --watch",
        "zip": "zip -r spelling-bee-extension.zip manifest.json popup.html dist/ icons/ words.js puzzles.js popup.css"
    },
    "dependencies": {
        "firebase": "^11.1.0"
    },
    "devDependencies": {
        "esbuild": "^0.24.2"
    }
}